<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/white"
        android:padding="24dp"
        android:layout_marginBottom="4dp"
        android:foreground="?selectableItemBackgroundBorderless"
        android:stateListAnimator="@animator/btn_soft">

        <TextView
            android:id="@+id/advertisement_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:textStyle="bold"
            app:layout_constraintEnd_toStartOf="@+id/star"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="Document_title" />

        <TextView
            android:id="@+id/advertisement_description"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:ellipsize="end"
            android:maxLines="3"
            android:textSize="14sp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/advertisement_title"
            tools:text="document_description" />

        <ImageView
            android:id="@+id/star"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:contentDescription="@string/contentDescription_icon"
            android:src="@drawable/star2"

            android:stateListAnimator="@animator/btn_soft"
            app:layout_constraintEnd_toEndOf="@+id/advertisement_description"
            app:layout_constraintTop_toTopOf="@+id/advertisement_title" />

        <TextView
            android:id="@+id/data_added"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:textSize="14sp"
            android:visibility="gone"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/advertisement_description" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>